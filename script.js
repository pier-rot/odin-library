const myLibrary = [];

function Book(title, author, pages, edition) {
  // the constructor...
  this.title = title;
  this.author = author;
  this.pages = pages;
  this.edition = edition;
}

function addBookToLibrary(book) {
  // do stuff here
}